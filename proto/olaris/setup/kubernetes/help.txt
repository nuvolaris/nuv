Subcommand: nuv setup kubernetes

Create, or destroy  the Nuvolaris deployment on a Kubernetes cluster.

Show the status of the cluster and the available contexts

Usage:
   kubernetes [options] create 
   kubernetes [options] destroy 
   kubernetes show (contexts|status)

Options:
   --context=<context>  select the kubectl context instead of the default one
